<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    
    <div class="container">
        <h1>Budgtr</h1>
        <table>
          <% Budget.forEach((element, index) => { %>
            
            <tr><%- helpers.createTableData(element, index) %> </tr>
            
          <%})%>
        </table>
    </div>
</body>
</html>